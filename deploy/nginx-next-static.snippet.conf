/**
 * nginx: serve accumulated hashed Next assets (see deploy/release.sh).
 * MUST use alias to static-assets/ — never wipe that dir with rm -rf.
 * Do NOT use try_files with alias (breaks %5Bid%5D paths like friends/[id]).
 *
 * location /_next/static/ {
 *     alias /var/www/blabla/static-assets/;
 *     access_log off;
 *     expires 365d;
 *     add_header Cache-Control "public, max-age=31536000, immutable";
 * }
 */
